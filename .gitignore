# 运行时工作区（含 session 留痕、产出、快照）——不入库
/workspace/

# 凭证：绝不允许提交
.env
*.key
auth.json
# QQBot 凭证（扫码登录的落点；也认 --credentials 指到别处）
qqbot.json
qqbot.json.bak

# Python
.venv/
__pycache__/
*.py[cod]
.ruff_cache/
.pytest_cache/
dist/
build/
*.egg-info/

# 本地临时
*.log

# 机器信息交接（IP / 账号 / 密码）——docs/deploy.md 明令不进公开仓库
.local/

# 部署/运维时用的一次性本地脚本与中间产物（不要进仓库）
.ssh_*.py
.push_creds.py
_journal_backup.md
